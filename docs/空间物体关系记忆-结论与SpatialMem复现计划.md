# 空间物体关系记忆：结论记录与 SpatialMem 复现计划

> 日期：2026-08-03
> 状态：研究结论已确认，复现工程启动中
> AI 披露：本文由 AI 辅助研究整理，结论均核对自文末来源

## 1. 问题重述

最初的问题是"第一视角设备如何通过视频流重建室内空间并记忆物品位置"。经讨论后收窄：

**真正需要的是"物体 + 属性 + 物体间关系"的可查询记忆，而不是像素级场景重建。**

典型需求示例："被子在客厅中央的桌子上"——需要：

1. 识别物体（被子、桌子、客厅）；
2. 识别物体所在位置（桌子在客厅中央）；
3. 记录物体之间的关联（被子在桌子上、桌子在客厅里）。

## 2. 核心结论

### 结论 1：需要"轻量几何"，不需要"场景重建"

| 层次 | 内容 | 对物品关系记忆是否必需 |
|---|---|---|
| 稠密场景重建 | 网格 / 3DGS 点云、光照、像素细节 | 不需要，纯算力与存储浪费 |
| 轻量几何 | 相机位姿 + 相对深度 + 全局竖直对齐 + 物体 3D 框/质心 | 很需要，保证跨视角一致性与"距离/方向"可度量 |
| 语义层 | 检测、VLM 嵌入、关系谓词、层级记忆 | 核心，即"物体+属性+关系" |

依据：SpatialMem 明确以"度量 3D 作为可解释的索引支架，而非显式建图目标"为设计原则，位姿/深度后端可插拔（VGGT / SLAM3R / COLMAP），不需要专用深度传感器。

### 结论 2：关系表示由关系类型决定——全局度量 vs 视角标签

| 维度 | 全局度量（allocentric） | 视角标签（egocentric） |
|---|---|---|
| 参考系 | 房间固定坐标系（地板平面 + 朝上 z 轴） | 用户当前头/身体朝向 |
| 稳定性 | 转头、走动都不变 | 转个身就变 |
| 能回答 | "被子在桌子中央""离窗户 2 米" | "在你左前方" |
| 代价 | 需要位姿+尺度对齐，误差会累积 | 轻量，单帧即可 |
| 适用关系 | 支撑/包含/邻近/上下等物理关系 | 左/右/前/后等指示关系 |

**二者互补而非二选一**：物理关系放全局坐标系判定（SpatialMem 的 `on` 定义为"物体底面与支撑面顶面平齐 + 投影重叠"，用 z 轴高度差判定）；指示关系按帧打标签、跨视角融合。回答用户时：先查全局层得到稳定事实，再按用户当前位姿做坐标变换输出行动指令（"在你左前方约两米"）。

### 结论 3：身份保持先于关系——发现与跟踪是最大瓶颈

- OVQ2D/ESOM（WACV 2026）消融：端到端在线成功率仅 ~4%；"发现+跟踪"补齐后达 81.9%。
- REMIND（2026）：无需位姿/深度的长期多物体再识别，IDF1 达 90.35%。
- 工程含义：先解决"同一物体跨帧认得出、跟得住"，再谈关系推理。

### 结论 4：更新策略——区分"用户移动"与"物体移动"，事件驱动而非每帧重写

- 用户走路/转头 → **不更新关系图**，只更新用户位姿（VIO），回答时做视角变换。
- 用户移动物体 → **更新关系图**，但需"离开旧支撑 + 被新支撑承接"双条件 + 多帧确认，避免遮挡/晃动误判；旧关系带时间戳保留（"被子之前放在沙发上"也可能是有效答案）。
- 实时性分层：
  - 帧级实时：位姿、检测、跟踪（端侧低功耗）；
  - 事件级异步：VLM 关系推理、记忆提交（多帧证据确认后，或场景切换/静置时批量刷新）。
- 建议显式存储用户自身轨迹（"最后一次经过桌子是 10:20"），对找东西与回忆都有价值。

## 3. 推荐记忆架构（SpatialMem 式）

```
房间(客厅)                                    ← 根：场景元数据/全局坐标系
 └─ 结构性锚点: 墙/门/窗(桌子的参照)            ← L1：几何(平面/框)+语义+置信度
     └─ 家具/物体: 桌子 {3D框, 类别, 嵌入, 证据裁剪图, 最后观察时间}
         ├─ 物体: 被子 {类别,颜色,尺寸,视觉嵌入,3D质心,最后观察时间,状态}
         └─ 关系边: on(被子,桌子,置信度0.9,时间戳) | near(桌子,窗)

每节点存：几何 G(v)、语义 S(v)、文本描述 D(v)
每节点带双层描述：
  Layer 1（图像级）：随当前帧变化，记录瞬时视图
  Layer 2（场景级）：多视角一致后才写入，保守更新防漂移
查询 = 沿图行走 + 谓词校验（距离/方向/可见性）
```

## 4. SpatialMem 复现计划

### 4.1 论文管线（按论文提取）

```
RGB 视频(egocentric)
  → 位姿 + 深度（可插拔后端：VGGT/SLAM3R/COLMAP）
  → 点云融合 → 竖直度量坐标系（地板平面拟合 + 朝向稳定 + 高度先验定尺度）
  → 结构性锚点：墙=大竖直平面；门/窗=竖直开口（多视角验证后提升为 L1 节点）
  → 物体节点：2D 检测/分割 → 提升到 3D 框（可选 SpatialLM 粗框先验，多视角精化）
  → 双层描述（属性 + 相对锚点/物体关系）
  → 有根树记忆（Root=房间, L1=锚点, L2=物体, L3=描述）
  → 查询：locate / relational query / 导航路径
```

### 4.2 组件映射：论文 → 我们的资产

| 论文组件 | 我们的可用资产 | 差距 |
|---|---|---|
| 位姿后端 | 录制设备 App 的 PoseSource（本次录制 pose.jsonl 为空，需开启或离线估计） | 需补 VIO 记录或 COLMAP/ORB-SLAM |
| 单目深度 | MiDaS v2.1 Small TFLite | 相对逆深度，需尺度恢复（论文用高度先验） |
| 2D 检测/分割 | EfficientDet-Lite0 / YOLO11n（真机已验证） | 需接分割（SAM）或检测框近似 |
| 锚点提取（墙/门/窗） | 无 | 需实现（RANSAC 竖直平面 + 开口检测） |
| 描述层（VLM/LLM） | 未定 | 需选型（端侧轻量 VLM 或云端 API） |
| 记忆树 + 谓词 + 查询 | 无 | 本次复现先行实现（纯 Python/NumPy） |
| 评测 | 无 | 对标 LongSpace-Bench / Ego4D VQ 设计 |

### 4.3 测试数据

- `glasses-recordings/session_20260803_125024_indoor_walk`：室内走动（办公室环境），60s，640×360@15fps，含视频时间线；
- 其余会话为手势/首次采集，可作补充；
- 注意：当前记录 pose.jsonl/imu.jsonl 均为空，需开启采集或离线估计位姿。

### 4.4 里程碑

1. **M0（2026-08-03 完成）**：复现工程骨架 + 记忆树/谓词/查询核心 + 数据准备脚本 + 冒烟测试；
2. **M1（2026-08-04 完成）**：几何层落地——COLMAP 位姿 + 度量点云 + 竖直对齐/高度先验定尺度；
3. **M2（2026-08-04~05 完成）**：锚点 + 物体提升——墙（RANSAC）、支撑面、物体 3D 框与实例关联
   （门/窗开口锚点未做，见 §4.5）；
4. **M3（2026-08-11 完成）**：描述层——属性/关系文本生成与双层合并
   （`src/spatialmem/descriptions.py` + `scripts/describe_memory.py`，颜色=HSV 启发式、
   尺寸=度量 3D 框、L2=多视角一致合并；**已升级为 qwen-vl-max + 跨帧共识**，HSV 停用）；
5. **M4（2026-08-11 完成）**：评测——LongSpace 风格 QA + 真实录制回放
   （`scripts/run_eval_qa.py` + `eval_vlm_compare.py` + `eval_multiview_quality.py`；
   cup_walk 实例级基线：locate@1=33%、VLM 命名核对 67%）；
6. **M5（规划中，2026-08-12 立项）**：无感持续学习 + 机会式确认——见 §4.6。

### 4.6 无感持续学习设计原则（M5，2026-08-12）

**目标**：用户零操作、后台不停地把"看到过的物体"沉淀为可检索记忆；
"扫一眼认不出来"是常态，记忆的价值在于把一次次的自然交互结果积累下来，
下次直接检索（对应 SpatialMem/ESOM 的 episodic object memory，计划 §2 结论 3：
身份保持先于关系）。

**核心原则：只积累证据，不急于下结论；结论靠多源一致，升级靠自然时机。**

| 机制 | 行为 | 用户负担 |
|---|---|---|
| 后台候选池 | 每秒/每帧做 novelty 检测：新实例（与已知节点嵌入距离 > 阈值）建低置信度候选，含裁剪图/3D 锚点/出现时段 | 零 |
| 机会式确认 | 用户提问/模型提到物体/OCR 读到印刷字/多帧一致 → 候选升级为确认节点，不弹窗不询问 | 自然发生 |
| 身份保持 | 同一物体跨帧、跨会话（放回再拿出）靠视觉嵌入/再识别认出来 | 零 |
| 检索优先问答 | 问"口红在哪"→ 先查记忆命中再答，附"根据记忆…"，不确认才扫当前画面 | 零 |
| 遗忘与纠正 | 旧/移动物体带时间戳归档（现有 archive 机制）；用户说"不对"即降级 | 一句话 |

**写入的数据模型（确认节点）**：`{标签, 颜色, 尺寸, 裁剪图/视觉嵌入, 3D 锚点,
来源(interactive|passive|ocr), 置信度分级, 首次/最近出现时间}`。

**关键难点与对策**：
1. 指代关联（用户问的到底是哪个物体）：说话附帧（客户端 attachBrainFrame 已有）
   + 画面焦点 + 文本线索；拿不准只记文本不记位置；
2. 记忆污染（模型答错就记错）：置信度分级——OCR 印刷字 > 视觉外观 > 猜测；
   低置信只进候选；保留纠正通道；
3. 触发策略：novelty 检测避免每帧重复建节点；
4. 隐私/功耗：全端侧、加密落盘、后台低频采样。

**M5 拆解**：
- M5.1 候选池 + novelty 检测（跨帧同实例合并）；
- M5.2 机会式确认（交互答案绑定候选 + OCR/多帧一致升级）；
- M5.3 检索优先问答（记忆命中先答，画面确认兜底）；
- M5.4 视觉再识别 + 遗忘/纠正（跨会话身份、时间戳归档）。

**验收**：新场景 M4 基线（待人工核对 GT）→ 注入"交互确认一条口红"→
模拟用户次日询问，验证命中率与"无感"（无任何用户记忆操作）。

### 4.5 未决问题

- 位姿后端选型：端侧 VIO vs 离线 COLMAP vs 学习式 VGGT/SLAM3R（需 GPU）；
- 描述层 VLM 选型与运行位置（端侧/手机/云端）；
- 谓词视角化基准：使用场景下"左/右/前/后"以用户朝向为准，需定义坐标系变换规范；
- 动态更新触发阈值与失效策略（"最后观察时间 + 状态变化"参数）。

## 5. 参考来源

1. Zheng, X., Liu, Y., Wu, C.-H., et al. *SpatialMem: Unified 3D Memory with Metric Anchoring and Fast Retrieval*. arXiv:2601.14895v2. https://ar5iv.labs.arxiv.org/html/2601.14895v2
2. Gu, Q., et al. *ConceptGraphs: Open-Vocabulary 3D Scene Graphs for Perception and Planning*. CVPR 2024 / arXiv:2309.16650. https://mila.quebec/en/conceptgraphs
3. Zhang et al. *EgoSG: Learning 3D Scene Graphs from Egocentric RGB-D Sequences*. CVPRW 2024.
4. Diaz-Pereda, P., et al. *REMIND: RE-Identification with Memory for INDoor Navigation*. arXiv:2607.09267.
5. Chen, B., et al. *SpatialVLM: Endowing Vision-Language Models with Spatial Reasoning Capabilities*. CVPR 2024 / arXiv:2401.12168.
6. Manigrasso, Z., et al. *Online Episodic Memory Visual Query Localization with Egocentric Streaming Object Memory*. WACV 2026 / arXiv:2411.16934.
7. *LongSpace: Exploring Long-Horizon Spatial Memory from Perception to Recall in Video*. arXiv:2606.05677.
8. Ego4D Consortium. *Episodic Memory Benchmark*. https://ego4d-data.org/docs/benchmarks/episodic-memory/
9. ZJUNLP. *LightMem-Ego: Your AI Memory for Everyday Life*. arXiv:2607.11487.
10. 蚂蚁灵波. *LingBot-Map*（InfoQ 报道, 2026-04）. https://www.infoq.cn/article/d2ODNNUg38yOQZyuUwUY
